let darkTheme = true;
let age = 23


//turnary Oerator in javascript
// let massage = condition ? Value for true; value for false;

let message = (age > 18) ? "yes you can give the vote" : "no you can't give vote"
let output = (age >= 18 && age <= 60) ? "yes you can apply for the license" : "no you can't apply for the license"

let output2 = (darkTheme === true) ? "darkTheme has applyed " : "darkTheme has stopped"

console.log(output)
console.log(output2)
